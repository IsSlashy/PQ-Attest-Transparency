/target
**/*.rs.bk
*.pdb
Cargo.lock.bak
.DS_Store

# web/pkg and web/sample-receipt.json are committed so the Vercel static deploy is self-contained
# (regenerate them with scripts/build-web-demo.sh).
/crates/pqtl-wasm/pkg-node

# On-chain (Solana/Anchor) build artifacts — rebuild via onchain/build-in-wsl.sh
/onchain/target
/onchain/.anchor
/onchain/test-ledger
/onchain/node_modules

.vercel
